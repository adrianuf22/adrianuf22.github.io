{
  "name": "adrianuf22.github.io",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build:posts": "node scripts/build-posts.js",
    "dev": "node scripts/build-posts.js && vite",
    "build": "node scripts/build-posts.js && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.2"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^18.0.11"
  }
}
